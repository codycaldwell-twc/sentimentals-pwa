import React from 'react';

export const Header = () => (
  <div className="App">
    <header className="App-header">
      <h1 className="App-title">Welcome to the Sentimentals Tone Analyzer</h1>
    </header>
  </div>
);

export default Header;
